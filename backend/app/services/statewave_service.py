class StatewaveService:
    """Placeholder service for patient contextual memory."""

    def save_patient_context(self, patient_id: str, payload: dict) -> dict:
        return {"patient_id": patient_id, "saved": True}
